# ============================================================
# Production env (On-premise / Ubuntu) — คัดลอกเป็น .env.production แล้วแก้ค่า
#   cp .env.production.example .env.production
#   docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
# ⚠️ ห้าม commit .env.production (อยู่ใน .gitignore)
# ============================================================

# --- App ---
NODE_ENV=production
PORT=5001

# --- MySQL (Docker Compose) ---
MYSQL_ROOT_PASSWORD=change_me_strong_root_password
MYSQL_DATABASE=damrongdham_db
MYSQL_USER=damrongdham_user
MYSQL_PASSWORD=change_me_strong_password

# --- Backend DB connection (ใช้ชื่อ service 'db' ใน compose network) ---
DB_HOST=db
DB_PORT=3306
DB_USER=damrongdham_user
DB_PASSWORD=change_me_strong_password
DB_NAME=damrongdham_db

# --- Backend / Auth ---
JWT_SECRET=change_me_random_64_chars
JWT_EXPIRES_IN=8h

# --- CORS ---
# ใส่ domain จริงของ server เช่น https://damrongdham.sisaket.go.th
CORS_ORIGIN=https://damrongdham.sisaket.go.th

# หมายเหตุ: บน Railway ไม่ใช้ไฟล์นี้ — ตั้งค่า env vars ใน Railway Dashboard แทน
# Railway env vars: NODE_ENV, PORT, DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME, JWT_SECRET, JWT_EXPIRES_IN, CORS_ORIGIN
