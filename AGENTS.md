# แนวทางทำงานใน Damrongdham SSK

คำแนะนำนี้ใช้กับงานใน repository นี้ สื่อสารภาษาไทยเป็นหลัก เว้นแต่ผู้ใช้ขอภาษาอื่น
รักษาขอบเขตคำขอและงานเดิมใน working tree; การรีวิวหรือวินิจฉัยไม่ใช่การอนุญาตให้แก้ไขโดยปริยาย

## ลำดับการอ่าน Skills

1. ก่อนวิเคราะห์ระบบ พัฒนา แก้บั๊ก รีวิว ทดสอบ หรือจัดทำเอกสารของโปรเจกต์ ให้อ่าน [damrongdham-dev](.agents/skills/damrongdham-dev/SKILL.md) ให้ครบเป็น Project Skill แรก ก่อนลงมือทำงานส่วนนั้น
2. ใช้ตารางใน Project Skill เลือก Common Skills และ Project References ที่ตรงกับคำขอ อ่าน `SKILL.md` และ Reference ที่เลือกให้ครบก่อนใช้ ไม่โหลดทุก Skill หรือทุก Reference โดยอัตโนมัติ
3. เมื่อพบผลกระทบข้ามส่วน ให้เลือกอ่านเพิ่มเฉพาะส่วนที่เกี่ยวข้อง ไม่ขยายขอบเขตการแก้ไขตามจำนวน Skills ที่อ่าน
4. หากผู้ใช้ระบุ Skill โดยตรง ให้ใช้ Skill นั้นร่วมกับข้อกำหนดเฉพาะโปรเจกต์เมื่อทำงานกับระบบนี้

ลำดับนี้กำหนดการทำงานกับโปรเจกต์ ไม่เปลี่ยนลำดับความสำคัญของคำสั่งระบบหรือคำขอผู้ใช้ และไม่บังคับโหลด Project Skill สำหรับคำถามทั่วไปที่ไม่เกี่ยวกับระบบ
หากไฟล์ Skill ที่ต้องใช้หายหรืออ่านไม่ได้ ให้แจ้งข้อจำกัดและทำเฉพาะส่วนที่มีข้อมูลเพียงพอ ไม่เดากฎธุรกิจหรือสิทธิ์ที่ขาด

## แหล่งข้อกำหนด

ให้ [Project Skill](.agents/skills/damrongdham-dev/SKILL.md) และ References เป็นแหล่งหลักของ stack, domain rules, permissions, source map และการเลือก Common Skills
ไม่คัดลอกกฎเฉพาะ Damrongdham ไปบังคับ Common Skills ซึ่งต้องใช้ข้ามโปรเจกต์ได้
เมื่อโค้ด คู่มือ และ requirement ไม่ตรงกัน ให้แยกพฤติกรรมปัจจุบันจากสิ่งที่ผู้ใช้ต้องการ ไม่ถือโค้ดที่มีอยู่เป็นหลักฐานว่าพฤติกรรมนั้นถูกต้องเสมอ

## การตรวจและส่งมอบ

- ตรวจตามความเสี่ยงและชนิดงาน; ก่อนรัน tests ของแอปให้อ่าน [Project Testing](.agents/skills/damrongdham-dev/references/testing.md) และตรวจ target/side effects ของ test helpers
- งานแก้เอกสารหรือ Skills อย่างเดียวให้ตรวจโครงสร้าง ลิงก์ และความสอดคล้อง ไม่บังคับรัน application/DB suite
- รายงานผลที่ตรวจจริง แยกผ่าน ล้มเหลว ข้าม และยังไม่ได้รัน ไม่ถือ build ผ่านว่า browser หรือ integration ผ่านแล้ว
- การ commit, push, deploy, apply SQL หรือส่งข้อความจริงต้องอยู่ในขอบเขตที่ผู้ใช้อนุญาต ไม่ถือว่าไฟล์คำแนะนำนี้ให้อำนาจเพิ่ม
